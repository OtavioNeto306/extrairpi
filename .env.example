# Configurações do Extrator RPI
# Copie este arquivo para .env e configure os valores conforme necessário

# Configurações de Log
# Níveis disponíveis: trace, debug, info, warn, error
RUST_LOG=info

# Configurações de Performance
# Tamanho máximo do arquivo PDF em MB (padrão: 50MB)
MAX_PDF_SIZE_MB=50

# Timeout para processamento de PDF em segundos (padrão: 30s)
PDF_PROCESSING_TIMEOUT=30

# Configurações de Desenvolvimento
# Porta para servidor de desenvolvimento (se aplicável)
DEV_PORT=3000

# Configurações de Build
# Ambiente de execução (development, production)
ENVIRONMENT=development

# Configurações de Segurança
# Chave para assinatura de dados (gere uma chave única para produção)
# APP_SECRET_KEY=your-secret-key-here

# Configurações de Cache
# Habilitar cache de resultados (true/false)
ENABLE_CACHE=false

# Diretório temporário para processamento
# TEMP_DIR=/tmp/extrair-rpi

# Configurações de Exportação
# Formato padrão de exportação (csv, json)
DEFAULT_EXPORT_FORMAT=csv

# Configurações de Interface
# Tema padrão da interface (light, dark, auto)
DEFAULT_THEME=light

# Configurações de Backup
# Habilitar backup automático de resultados
ENABLE_AUTO_BACKUP=false

# Diretório para backups
# BACKUP_DIR=./backups